services:
  postgres:
    volumes:
      - /srv/dushengcdn/postgres-data:/var/lib/postgresql/data

  dushengcdn:
    ports:
      - "127.0.0.1:3010:3000"
    environment:
      LOG_LEVEL: info
    volumes:
      - /srv/dushengcdn/data:/data
