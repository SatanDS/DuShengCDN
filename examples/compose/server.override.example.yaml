services:
  postgres:
    volumes:
      - /srv/dushengcdn/postgres-data:/var/lib/postgresql/data

  dushengcdn:
    ports:
      - "3010:3000"
    environment:
      LOG_LEVEL: info
    volumes:
      - /srv/dushengcdn/data:/data
