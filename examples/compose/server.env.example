DUSHENGCDN_VERSION=replace-with-release-version
DUSHENGCDN_REPO_DIR=../..
DUSHENGCDN_HTTP_BIND=0.0.0.0
DUSHENGCDN_HTTP_PORT=3010

POSTGRES_DB=dushengcdn
POSTGRES_USER=dushengcdn
POSTGRES_PASSWORD=replace-with-strong-password

# Required in release mode. Generate with: openssl rand -hex 32
SESSION_SECRET=
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_SAME_SITE=lax
# Default JSON request body limit. Binary uploads and DNS Worker heartbeat have dedicated limits.
DUSHENGCDN_JSON_BODY_MAX_BYTES=2097152
# Optional bootstrap password used only when the database has no users yet.
# Generate a strong value for first install, then remove or rotate it after login.
DUSHENGCDN_INITIAL_ROOT_PASSWORD=
# Comma-separated reverse proxy IP/CIDR ranges trusted for X-Forwarded-For.
# Leave empty unless the Server is behind a controlled proxy.
# TRUSTED_PROXIES=127.0.0.1,10.0.0.0/8
SQLITE_PATH=/data/dushengcdn.db
DSN=postgres://dushengcdn:replace-with-strong-password@postgres:5432/dushengcdn?sslmode=disable
DATABASE_MAX_OPEN_CONNS=30
DATABASE_MAX_IDLE_CONNS=10
DATABASE_CONN_MAX_LIFETIME_SECONDS=1800

GIN_MODE=release
LOG_LEVEL=info

# Optional DNSSEC private-key encryption key. Required before enabling
# Authoritative DNS DNSSEC. Use the same value on Server and DNS Worker.
# Generate with: openssl rand -base64 48
DUSHENGCDN_DNSSEC_KEY_ENCRYPTION_KEY=

# Optional compatibility token for old Agents deployed before node-specific tokens.
# New deployments should use discovery_token or node-specific agent_token instead.
# Keep disabled unless you are in a short migration window.
# DUSHENGCDN_AGENT_LEGACY_GLOBAL_TOKEN_ENABLED=false
# AGENT_TOKEN=replace-with-legacy-agent-token

# Optional Redis backend for rate limit/session-related runtime helpers.
# REDIS_CONN_STRING=redis://redis:6379/0
# Set to true when production must fail startup instead of falling back to in-process helpers.
# REDIS_REQUIRED=false

# Optional commercial license enforcement for private/commercial deployments.
# DUSHENGCDN_LICENSE_REQUIRED=false
# DUSHENGCDN_LICENSE_PUBLIC_KEYS=base64url-or-hex-ed25519-public-key
# DUSHENGCDN_LICENSE_ALLOW_UNSIGNED=false
DUSHENGCDN_LICENSE_ACTIVATION_URL=https://www.satandu.com
DUSHENGCDN_LICENSE_ONLINE_ACTIVATION_REQUIRED=true
DUSHENGCDN_LICENSE_LEASE_DURATION_HOURS=72
DUSHENGCDN_LICENSE_LEASE_RENEW_BEFORE_HOURS=6

# Optional Server automatic binary upgrade. Keep disabled unless the release
# supply chain and rollback plan are verified.
# DUSHENGCDN_SERVER_AUTO_UPGRADE_ENABLED=false
